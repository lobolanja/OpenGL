#include <iostream>
#include "stdlib.h"
#include "stdio.h"
#include <GL/glut.h>
#include <ctype.h>
#include <vector>
#include <vertex.h>
#include "dibujo.cc"
struct ovni{ 
	//~ vector<dibujo*> partes;
	//~ bool revolu = false;// cambiar en el inicializador+++++
	void aniadir_parte(dibujo* aniade){
		//partes.push_back(aniade);
	}
	//~ void revolucionar(){
		//~ for(int i = 0; i < partes.size(); i++){
			//~ partes[i]->revolucion();
		//~ }
		//~ revolu = true;
	//~ }
	//~ void draw_ovni(bool ajedrez = false){
		//~ if(!revolu){
			//~ revolucionar();
		//~ }
//~ 
		//~ for(int i = 0; i < partes.size(); i++){
			//~ partes[i]->draw_objects(ajedrez);
		//~ }
	//~ }
	//~ 
};
